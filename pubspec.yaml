name: miracle
description: A library for interacting with miracle-wm.
version: 2.3.0
repository: https://github.com/miracle-wm-org/miracle.dart

environment:
  # `package:ffi`, which the configuration bindings need, requires 3.7.0.
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  ffi: ^2.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
